
# Installation: pip install -r requirements.txt

pandas>=2.0.0
numpy>=1.24.0
scipy>=1.10.0
statsmodels>=0.14.0
matplotlib>=3.7.0
seaborn>=0.12.0
pyreadr>=0.4.7

# Alternative R interface (if pyreadr doesn't work)
# rpy2>=3.5.0

# Machine learning (for train-test split and preprocessing)
scikit-learn>=1.3.0

# Jupyter notebook support (optional, for interactive analysis)
jupyter>=1.0.0
notebook>=7.0.0

# Progress bars (optional, for long operations)
tqdm>=4.65.0
